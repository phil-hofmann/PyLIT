import unittest

class TestUtils(unittest.TestCase):

    """ Test the utils module. 
    
    Notes
    -----
    The following method is just a dummy method."""
    
    def test_multi_index_set(self):

        """ Test the multi_index_set function.
        
        Notes
        -----
        1. TODO: test if the dimension is correct, i.e. the length of the multi-indices.
        2. TODO: test if the degree is correct, i.e. the maximum value of the multi-indices.
        3. TODO: test if the cardinality is correct, i.e. the number of multi-indices equals (dergee+1)**dimension.
        4. TODO: test if every multi-index is unique.
        5. TODO: test if every multi-index is non-negative.
        6. TODO: test if the multi-indices are in the correct order.
        7. TODO: test if the multi-indices are of the correct type. (ARRAY)
        8. TODO: test an edge case, e.g. dimension=0, degree=0.
        9. TODO: test an example with a known solution."""

        self.assertTrue(True)

if __name__ == '__main__':
    
    unittest.main()